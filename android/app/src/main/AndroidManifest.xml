<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Downloading a multi-hundred-MB model needs a foreground service,
         otherwise Android kills the transfer in the background. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- The practice session records the child's voice to drive the live
         audience and compute paraverbal metrics. Audio never leaves the
         device and is discarded after analysis. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Optional second modality: the front camera reads body language
         (looked at the audience, smiled) via on-device ML Kit. Video is never
         recorded — frames are analysed on the fly and discarded. The camera is
         not required; without it the session runs audio-only. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:label="OratorIA Kids"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <!-- GPU delegate for on-device inference. This is the single biggest
             performance lever: without OpenCL the model falls back to CPU,
             where a short answer takes ~10 s instead of under a second.
             android:required="false" so devices without OpenCL still install. -->
        <uses-native-library android:name="libOpenCL.so" android:required="false"/>
        <uses-native-library android:name="libvndksupport.so" android:required="false"/>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
